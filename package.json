{
  "name": "luke-lachlan-day.github.io",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "check:assets": "node scripts/check-assets.mjs",
    "audit:assets": "node scripts/check-assets.mjs",
    "process:assets": "node scripts/process-assets.mjs",
    "build": "astro build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test:e2e": "playwright test",
    "validate": "npm run check && npm run build && npm run format:check && npm run audit:assets && npm run test:e2e",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.59.1",
    "prettier": "^3.8.3",
    "prettier-plugin-astro": "^0.14.1",
    "typescript": "^6.0.3"
  }
}
