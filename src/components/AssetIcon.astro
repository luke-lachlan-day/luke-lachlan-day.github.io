---
interface Props {
	src: string;
	class?: string;
}

const {
	src,
	class: className,
} = Astro.props;
---

<span
	class:list={['asset-icon', className]}
	style={`--icon-url: url("${src}")`}
	aria-hidden="true"
></span>
