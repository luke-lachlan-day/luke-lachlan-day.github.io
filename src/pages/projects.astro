---
import BaseLayout from '../layouts/BaseLayout.astro';
import { profile } from '../data/profile';

const { projects, companies } = profile;
const companyById = new Map(companies.map((company) => [company.id, company]));
---

<BaseLayout
	title={projects.title}
	description={projects.description}
>
	<section class="page-hero" aria-labelledby="projects-title">
		<h1 id="projects-title">{projects.hero.title}</h1>
		<p>{projects.hero.lead}</p>
	</section>

	<section class="section" aria-labelledby="project-list-title">
		<div class="section-heading split">
			<div>
				<p class="eyebrow">{projects.eyebrow}</p>
				<h2 id="project-list-title">{projects.listTitle}</h2>
			</div>
			<p>{projects.copy}</p>
		</div>

		<div class="project-grid">
			{
				projects.items.map((project) => {
					const company = companyById.get(project.companyId);
					const picture = project.pictures[0];

					return (
						<a
							class="project-card"
							href={projects.href}
							aria-label={`View ${project.product} in projects`}
						>
							<img class="project-image" src={picture.src} alt={picture.alt} loading="lazy" />
							<div class="project-body">
								<div class="project-company">
									{company && <img src={company.icon.src} alt="" loading="lazy" />}
									<span>{company?.name}</span>
								</div>
								<h3>{project.product}</h3>
								<p>{project.description}</p>
								<div class="tool-list" aria-label={`${project.product} tools`}>
									{project.tools.map((tool) => <span>{tool}</span>)}
								</div>
							</div>
						</a>
					);
				})
			}
		</div>
	</section>
</BaseLayout>
