---
import BaseLayout from '../layouts/BaseLayout.astro';
import { profile } from '../data/profile';

const { timeline, projects } = profile;
---

<BaseLayout
	title={timeline.title}
	description={timeline.description}
>
	<section class="section" aria-labelledby="projects-title">
		<div class="section-header">
			<p class="eyebrow">{projects.eyebrow}</p>
			<h2 id="projects-title">{projects.title}</h2>
			<p>{projects.copy}</p>
		</div>

		<div class="project-grid">
			{
				projects.items.map((project, index) => (
					<article class="project-card">
						<span class="meta-label">slot {String(index + 1).padStart(2, '0')}</span>
						<h3>{project.title}</h3>
						<p>{project.copy}</p>
					</article>
				))
			}
		</div>
	</section>

	<section class="section" aria-labelledby="history-title">
		<div class="section-header">
			<p class="eyebrow">{timeline.history.eyebrow}</p>
			<h2 id="history-title">{timeline.history.title}</h2>
		</div>

		<div class="timeline">
			{
				timeline.items.map((item) => (
					<article class="timeline-item">
						<p class="timeline-date">{item.date}</p>
						<h3 class="timeline-title">{item.title}</h3>
						<p class="timeline-copy">{item.copy}</p>
					</article>
				))
			}
		</div>
	</section>
</BaseLayout>
